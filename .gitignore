# unrelated sibling projects living on disk in this folder (not part of this repo)
/hengo/
/money-flow/

# IDE
/.idea/
/.vscode/

# dependencies
/node_modules/

# next.js
/.next/
/out/

# production
/build/

# env
.env
.env*.local
.env.local*
.env*.backup*

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# vercel
# `vercel link` also appends `.env*`, which would shadow the tracked
# `.env.example`. The `.env` / `.env*.local` rules above already cover the
# files that must never be committed.
.vercel
/.swc
