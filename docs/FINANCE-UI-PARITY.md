# Finance UI parity

This audit compares the current `money-flow` reference checkout with the native
Luyra Finance routes. Money Flow is treated only as a UX and interaction
reference. Luyra authentication, API routes, services, repositories, and Neon
tables remain authoritative.

Status meanings:

- `MATCH`: the Luyra experience covers the useful Money Flow behavior.
- `PARTIAL`: the route works, but important hierarchy or interactions differ.
- `MISSING`: the behavior is absent.
- `DEFERRED`: intentionally postponed because the supporting domain is absent or
  outside the current screen.
- `NOT_NEEDED`: intentionally excluded from native Luyra Finance.

| Area | Money Flow source | Luyra destination | Feature | UX status | Backend status | Action |
|------|-------------------|----------------------|---------|-----------|----------------|--------|
| Finance shell | `components/layout/Sidebar.tsx`, `TabBar.tsx`, `NavShell.tsx` | `app/(app)/finance/layout.tsx` inside the Luyra `AppShell` | Global navigation | NOT_NEEDED | NOT_NEEDED | Keep the Luyra shell and only adapt Finance content. |
| Finance navigation | Money Flow top-level routes | `app/(app)/finance/layout.tsx` | Overview, Transactions, Budgets, Savings, Analytics, Review, Subscriptions, Settings | MATCH | DONE | Keep one Finance module with native subroutes. |
| Overview summary | `dashboard/components/SummaryCards.tsx` | `/finance` | Income, expense, net cash flow, savings rate | MATCH | DONE | Implemented with native aggregates, month controls, amount hierarchy, and semantic states. |
| Overview daily budget | `dashboard/components/DailyBudgetPill.tsx` | `/finance` | Today spend, remaining monthly budget, daily guide | MATCH | DONE | Implemented in the native service using current-month budget and daily spending data. |
| Overview budget alerts | `dashboard/components/BudgetAlerts.tsx` | `/finance` | 80%, 90%, and exceeded states | MATCH | DONE | Implemented with Watch, Near limit, and Exceeded labels plus accessible progress. |
| Overview categories | `dashboard/components/CategoriesPanel.tsx` | `/finance` | Category icon, spend, share, and budget relationship | MATCH | DONE | Implemented with native category metadata, expense share, and budget usage. |
| Overview trends | `dashboard/components/TrendsPanel.tsx` | `/finance` | Daily spending trend | MATCH | DONE | Implemented with a native daily aggregate and dependency-free accessible SVG chart. |
| Overview recent activity | `dashboard/components/RecentActivity.tsx` | `/finance` | Recent transaction hierarchy and View all link | MATCH | DONE | Implemented through native camel-case DTOs and `/finance/transactions`. |
| Overview review prompt | `dashboard/components/BudgetReviewPrompt.tsx` | `/finance` | Monthly review entry point | MATCH | DONE | Implemented when the selected month contains finance activity. |
| Overview deterministic insight | `dashboard/components/Intelligence.tsx` | `/finance` | Forecast and savings-rate guidance | DEFERRED | PARTIAL | Revisit after comparison/forecast fields are part of the native domain response. |
| AI Money Coach | `dashboard/components/MoneyCoach.tsx` | `/finance` | Generated recommendations and actions | DEFERRED | MISSING | Do not port AI endpoints, generated copy, or browser Supabase mutations. |
| Dashboard quick add | `dashboard/components/Intelligence.tsx` | `/finance` | AI-generated quick transaction entry | DEFERRED | MISSING | Still AI-only scope here — see the new Transaction templates row for the deterministic version, which is done. |
| Transaction list | `transactions/page.tsx`, `TransactionGroupList.tsx` | `/finance/transactions` | Compact rows, date groups, aligned amounts | MATCH | DONE | Date grouping, income/expense day subtotals, and the dense row hierarchy are implemented natively. |
| Transaction search and filters | `SearchBar.tsx`, `FilterPanel.tsx`, `useTransactionFilters.ts` | `/finance/transactions` | Search, type, date, category, amount, sort | MATCH | DONE | Server-side search, type, category, payment method, amount range, and sort are implemented with parameterized SQL. Free date-range filtering is intentionally superseded by the month selector (`NOT_NEEDED`) rather than duplicating scope. |
| Add/edit transaction | `components/transactions/AddTransactionSheet/**` | `/finance/transactions` | Mobile sheet, amount-first form, category and payment method | MATCH | DONE | Amount-first entry, category chips, and payment method are DTO-backed. Sticky footer keeps Save visible above the keyboard. The shared `Input`/select primitives are still 36px tall (below the 44px target); revisit as part of a cross-Finance touch-target pass rather than a Transactions-only patch. |
| Swipe transaction actions | `components/transactions/SwipeableRow.tsx` | `/finance/transactions` | Safe mobile edit/delete with visible alternatives | DEFERRED | DONE | Row tap-to-edit, dropdown menu (Edit/Delete), and keyboard access all work today. A real swipe gesture needs on-device testing to get right (no test harness in this repo) and Money Flow itself lists it as an optional enhancement — deferred rather than shipped unverified. |
| Recurring rules | `components/transactions/RecurringSheet.tsx` | `/finance/transactions` | Create and manage recurrence | DEFERRED | MISSING | Do not port the old recurring backend; expose no fake success path. Explicitly skipped by request — Templates (Phase 9) was done in its place while Recurring (Phase 8) stays deferred. |
| Transaction templates | `components/transactions/AddTransactionSheet/TemplateStrip.tsx`, `useTemplates.ts` | `/finance/transactions` | Save, apply, and delete quick-entry templates | MATCH | DONE | `finance_transaction_templates` existed in `004_finance.sql` since the original migration with zero code consumers — now a full native CRUD feature: a horizontal template strip in the Add Transaction sheet (create mode only) that fills the form on tap (never auto-submits), a "Save as template" action, and per-template delete. Capped at 12 templates per user (`finance-transaction-template-service.ts`) so the strip stays scannable — templates are for speed, not a second transaction list. Templates stay KRW-only: the table's `currency` column exists but the table has no `amount_usd`/`exchange_rate` columns to hold a USD template correctly, so wiring it would need its own migration — deliberately not done as part of Phase 10 below. |
| Multi-currency entry | `AddTransactionSheet` currency/USD fields, `app/api/exchange-rate/route.ts` | `/finance/transactions` | Enter a transaction in USD with live/manual exchange rate | MATCH | DONE | `finance_transactions.currency`/`amount_usd`/`exchange_rate` and the global `finance_exchange_rates` cache table existed in `004_finance.sql` with zero code consumers — same "dead migrated column" pattern as Settings and Templates. KRW stays the one canonical accounting amount everywhere (all sums, budgets, analytics); a transaction may additionally carry `currency: "USD"` + `originalAmount` + `exchangeRate`, and `amountKrw = round(originalAmount * exchangeRate)` is computed once, server-side, in `finance-transaction-service.ts` — the API never trusts a client-computed KRW figure for a USD entry. `GET /api/finance/exchange-rate` fetches live (exchangerate-api.com, gated behind the optional `EXCHANGE_RATE_API_KEY` env var) with a 60-minute DB cache (`finance_exchange_rates`), falling back to a fixed 1370 rate — matching Money Flow's own fallback constant — when no key is configured; the rate field is always user-editable regardless, satisfying "allow manual rate fallback". Transaction rows show the original USD amount and rate alongside the canonical KRW amount when present. Savings goals' pre-existing USD-native `targetUsd`/`currentUsd` fields are a separate, already-working pattern and were not touched. |
| Budget overview | `budget/page.tsx` | `/finance/budgets` | Total budget, spent, remaining, warning count | MATCH | DONE | Native summary reuses the deterministic `toBudgetPerformance` figures already computed for Overview's budget alerts. |
| Budget category cards | `budget/page.tsx` | `/finance/budgets` | Progress, remaining, healthy/watch/near/exceeded | MATCH | DONE | Compact cards with icon, progress bar, and a text+icon status badge (never color alone) for all four states. Create/edit/remove moved from always-visible inline fields to a Sheet + confirm dialog. Budgets remain a standing per-category limit (no month column on `finance_budgets`); the month selector changes which month's spend is measured against that limit, matching the existing backend model rather than introducing per-month budgets. |
| Category types | `components/budget/CategoryTypesSheet.tsx` | `/finance/budgets` | Spending class assignment | DEFERRED | MISSING | Do not port browser Supabase category classification without a native domain. |
| Merchant aliases | `components/budget/MerchantNamesSheet.tsx` | `/finance/budgets`, `/finance/subscriptions` | Suggested and confirmed merchant groups | DEFERRED | MISSING | Keep exact-description subscription detection until a native alias domain exists. |
| Savings summary | `savings/page.tsx` | `/finance/savings` | Total saved and overall progress | MATCH | DONE | Total saved, total target, overall progress, and goals-reached metric cards, computed from native goal totals (no month scope — savings goals are not month-bound). |
| Savings goal cards | `savings/page.tsx` | `/finance/savings` | Progress, remaining, target date, actions | MATCH | DONE | Compact cards (icon, progress, remaining/target-date, reached badge) with Sheet-based create/edit and a contribution Sheet that also lists recent contribution history via the existing `GET .../contributions` route. Create/edit/delete moved off always-visible inline forms. Fixed a pre-existing bug in `db/migrations/004_finance.sql` where deleting a goal with any contributions raised a foreign-key violation instead of cascading — see `db/migrations/007_finance_savings_contributions_cascade.sql` (not yet applied to the database; needs a manual `psql`/Neon-console run per `db/README.md`). |
| Savings coach and auto-plan | `SavingsCoach.tsx` | `/finance/savings` | Generated plan and automatic deposits | DEFERRED | MISSING | Do not port AI or unsupported automation. Money Flow's planned/auto-monthly contribution fields (`auto_monthly_usd`, `reminder_day`, `skipped_month`) have no native column equivalent — deferred to Phase 8 (recurring infrastructure) rather than added ad hoc. |
| Analytics monthly view | `analytics/MonthlyView.tsx` | `/finance/analytics` | Selected-month summary | MATCH | DONE | Income/expenses/net/savings-rate metric cards plus a dedicated month-over-month comparison card (deltas, not just this month's numbers). |
| Analytics trends | `analytics/TrendsView.tsx` | `/finance/analytics` | Income vs expense, net flow, categories, payment methods, forecast | PARTIAL | DONE | Cash-flow trend (income+expense lines, one shared axis, hover tooltip, legend), category distribution and payment-method breakdown (bar lists — reuse each category's own `color`; payment methods use a new fixed-order validated categorical palette, `app/globals.css` `--chart-series-*`, colored by a stable hash of the method's id so color never depends on amount rank). Deliberately **no Recharts**: every target chart was buildable with the existing dependency-free SVG/bar-list approach at equal quality, so the pre-approved dependency wasn't "genuinely needed" per the phase instructions. Forecast and Budget-vs-Actual are intentionally not ported — the native target list for this screen doesn't include them, and Budget-vs-Actual would duplicate `/finance/budgets`. |
| Monthly review | `review/page.tsx` | `/finance/review` | Totals, prior month, category and budget performance | MATCH | DONE | Full hierarchy: summary cards, budget performance (shared `BUDGET_STATUS_META`, see below), "Where spending changed" (top swings by absolute delta), Subscriptions, Savings progress, and a **Key observations** section — deterministic findings (category swings ≥15%, budgets exceeded, savings-rate change) computed in `buildReviewObservations()` in the service layer, never in JSX and never sent to an LLM. |
| Review recommendations | `review/page.tsx` | `/finance/review` | Apply next-month recommendations | DEFERRED | MISSING | Do not port direct Supabase writes or generated plans. |
| Subscriptions summary | `subscriptions/page.tsx` | `/finance/subscriptions` | Monthly/yearly recurring cost | MATCH | DONE | Monthly cost, yearly cost, active count, and marked-for-cancellation count as metric cards. |
| Subscription decisions | `subscriptions/page.tsx` | `/finance/subscriptions` | Keep, review, plan to cancel, cancelled | MATCH | DONE | Cards show merchant, latest/average/yearly amount, frequency, last charge, and category (via `CategoryIcon`, now carrying `categoryIcon`/`categoryColor` on `DetectedSubscription`). Status changes moved from four always-visible buttons to a status-badge dropdown (icon + label + color, consistent with Budgets/Review); cancelled items sort to the bottom instead of a flat unordered list. |
| Weekly summary | `lib/finance/weekly-checkin.ts` | `/finance/settings` (Reports section) | Income, expenses, net, top categories, budget warnings, savings progress, subscriptions, vs last week | MATCH | DONE | `getWeeklySummary()` in the new `finance-report-service.ts` — a rolling trailing-7-days-ending-yesterday window (Asia/Seoul), not a Mon–Sun calendar week, since no weekly concept existed natively to anchor to. Budget warnings check against the *current month's* standing budgets (budgets are monthly, matching Money Flow's own weekly-checkin behavior). Can now be pushed to Telegram on demand — see the Telegram row below. |
| Monthly report | `lib/finance/monthly-report.ts` | `/finance/settings` (Reports section) | Income, expenses, savings, savings rate, budget performance, top categories, month comparison, subscriptions, savings goals, important changes | MATCH | DONE | `getMonthlyReport()` composes the already-deterministic `getReviewSummary()` rather than re-deriving the same month's totals a second time — Review and the Monthly Report must never disagree on a number, so they share one computation, adding only a ranked/percentaged top-categories list on top. "Important changes" reuses Review's existing `buildReviewObservations()` rather than porting Money Flow's separate positive/warning/action insight triplet — one native "observations" concept, not two competing ones. Surfaced inside Finance Settings (not a new nav tab) per explicit direction, since AGENTS.md's Finance nav diagram lists exactly 8 tabs with no Reports entry. |
| Telegram integration | `app/api/telegram/route.ts`, `lib/telegram.ts`, `telegram_accounts` table | `/api/telegram/webhook`, `/api/telegram/link`, `/api/finance/telegram/send`, Finance Settings "Telegram" section | Link account, budget alert, weekly report, monthly report | PARTIAL | PARTIAL | **Built**: account linking (webhook-validated, secret-token gated, one chat per user via a unique index — new `telegram_accounts` table, deliberately account-level rather than Finance-scoped per AGENTS.md's "it is an integration, not tied to Luyra auth"), a 6-char linking code (15-min TTL, excludes ambiguous `0/O/1/I`) redeemed via a Telegram deep link, and **manual** "Send to Telegram" for the Weekly Summary / Monthly Report (a "Send" button in Settings — reuses the exact same report data Review/Settings already show, formatted as an HTML-parse-mode Telegram message with all text escaped via `escapeTelegramHtml()`). A minimal in-memory `lib/rate-limit.ts` guards the public webhook and the link-code endpoint, matching Money Flow's own abuse-guard pattern (best-effort only — noted in the code as not a hard guarantee across serverless instances). **Deliberately not built**: (1) scheduled/automatic delivery — Money Flow's weekly/monthly pushes and budget-alert threshold-crossing detection all run from `CRON_SECRET`-gated cron jobs, and Luyra has zero cron infrastructure (no `app/api/cron/**` anywhere) to hook into; the send routes exist and work today via manual trigger, but wiring a scheduler is a deployment-specific decision for later. (2) Expense logging by chat text — Money Flow parses free text with an AI provider and inserts a transaction directly; Luyra has no AI Foundation yet (Phase 15, not started) and AGENTS.md's AI rule is that free text never writes financial data without a deterministic, confirmed path, so this needed its own phase rather than an ad hoc parser bolted onto the webhook. The webhook still resolves every action through `getUserIdForChat()` — a DB lookup by `chat_id`, exactly Money Flow's security pattern — and replies "I can't log expenses yet" for anything it doesn't recognize, rather than silently doing nothing or guessing. Needs a real bot token (`TELEGRAM_BOT_TOKEN`, from @BotFather) and a registered webhook (`TELEGRAM_WEBHOOK_SECRET`) to do anything live — both optional env vars; without them, Settings just says "Telegram integration isn't configured." |
| Email reports | `monthly_report_email`/`monthly_report_channel_email` columns in `004_finance.sql`, Money Flow's Resend-based email delivery | `app/api/finance/email/{send,status}`, Finance Settings "Email reports" section | Weekly/monthly report delivery by email | PARTIAL | PARTIAL | **Built**: `db/migrations/009_finance_report_email.sql` renames the dead `monthly_report_email` column to `finance_report_email` (now shared by both report types, not monthly-only) and adds a new `weekly_report_channel_email` column, giving weekly and monthly independent toggles as AGENTS.md asks for — Money Flow itself has no weekly-email equivalent (Telegram-only for weekly there), so this goes beyond a strict port. A generic `lib/email/client.ts` (`isEmailConfigured()`/`sendEmail()`, mirroring `lib/telegram/client.ts`'s shape) wraps the official `resend` SDK — the one new dependency this phase adds, matching the "official SDK over raw fetch" precedent already set for Telegram/Anthropic. `finance-email-service.ts` formats the same deterministic `WeeklySummary`/`MonthlyReport` data as hand-rolled, escaped HTML (no `@react-email/components` — a plain escaped-string template was already the established pattern for the Telegram message and needed no extra dependency) and resolves the recipient as `financeReportEmail` (an explicit override) falling back to the user's Supabase account email, so email delivery works with zero setup beyond enabling a toggle — no forced "type your own email" step. **Deliberately not built**: scheduled/automatic sending — same "no cron infrastructure" reasoning as the Telegram phase; the weekly/monthly toggles record delivery *preference* for whenever a scheduler is deployed, and the manual "Send email" button (mirroring "Send to Telegram", gated on a new `/api/finance/email/status` check rather than a link status since there's no linking step) is the only way a report actually goes out today. Needs `RESEND_API_KEY` (from resend.com) to do anything live — `RESEND_FROM_EMAIL` is an optional verified-domain override; without either, Settings just says "Email isn't configured." |
| Finance settings | supported parts of `settings/**` | `/finance/settings` | Spending limit and target savings rate | MATCH | DONE | Financial goals (unchanged), plus previously-dead capabilities newly wired end-to-end: Budget controls (watch/near-limit thresholds, replacing the hardcoded 80/90 in `finance-budget-service.ts` — now consumed by Budgets, Review, *and* Overview's budget alerts), Reviews (`monthlyReviewEnabled`, gating `/finance/review` itself), and Email reports (see the row above). All read/write `finance_preferences` columns — most existed in `004_finance.sql` since the original migration with zero code consumers until wired up across sessions; email needed one small additive migration (`009_finance_report_email.sql`). The "weekly review" toggle (distinct from `monthlyReviewEnabled`) stays out: no weekly-review feature exists anywhere in Luyra to gate, so exposing it would be a toggle with no observable effect. Currency stays out per the mission's own phase order (Phase 10). Login, password, appearance, and AI settings correctly stay out of Finance Settings entirely, as instructed. |
| Data export (CSV/JSON) | `app/api/export/route.ts`, `DataSafetySection.tsx` | `/finance/settings` (Export section), `app/api/finance/export/route.ts` | Download all transactions as CSV or JSON | MATCH | DONE | Scoped to all-time transactions, matching Money Flow's authoritative `/api/export` route — not its second, weaker Analytics-page CSV exporter, which the research found has **no formula-injection escaping at all**; that gap was not reproduced. `csvCell()` in `lib/finance-csv.ts` ports the exact safe pattern: a leading `= + - @` (or tab/CR) gets a literal `'` prefix so Excel/Sheets treats the cell as text instead of a formula, and every free-text cell (description, category/payment-method name, note) is RFC-4180 quoted with internal `"` doubled. Only free-text fields go through `csvCell` — date/type/amounts/currency/rate are numbers or controlled enum strings, never user-authored, so they're written raw (matching the reference). JSON export is a dedicated `TransactionExportRow` DTO (camelCase, per this app's own convention rather than Money Flow's snake_case), wrapped in an `{ exportedAt, count, transactions }` envelope. Neither export includes budgets/savings/subscriptions — Money Flow doesn't either. |
| Printable report | `DataSafetySection.tsx` `handleExportPDF()` | `/reports-print` (linked from the Reports section's Print button) | Print / Save as PDF | MATCH | DONE | Deliberately **not** Money Flow's pattern of `window.open('', '_blank')` + `document.write(html)` with transaction fields interpolated unescaped into an HTML string — the research flagged that as an actual XSS-style risk. Instead, a real Next.js route (`app/reports-print/`, placed outside the `(app)` route group so it renders with only the root `<html>/<body>` layout — no AppShell chrome, no Finance tab nav — a clean page for printing) renders the same `WeeklySummaryCard`/`MonthlyReportCard` components used in Settings through normal JSX, which auto-escapes all text content. A "Print / Save as PDF" button calls `window.print()`; no PDF library was added, per the phase's own instruction to prefer a print-friendly page. Prints the *report* (Weekly Summary or Monthly Report), not a raw transaction dump — Money Flow's own PDF path just happened to reuse its JSON transaction export because it had no equivalent report screen; Luyra does, so printing the report is the more useful port of the underlying idea. |
| AI Money Coach | `dashboard/components/MoneyCoach.tsx` | `/finance/settings` ("AI Money Coach" section), `app/api/finance/money-coach/route.ts` | Deterministic-facts-in, explanation-and-suggestion-out, confirmed budget-change recommendation | MATCH | DONE | Built the shared abstraction AGENTS.md asked for first: `lib/ai/client.ts` wraps the official `@anthropic-ai/sdk` (the one new dependency this whole migration has added — required, since there's no way to call an LLM provider without an SDK or raw HTTP, and the project's own Claude-API skill mandates the SDK over raw fetch) behind a single `callClaudeWithTool()` — no Finance-specific provider code, so Learning/Interview/Coach can call the same function later. The Finance-specific piece, `finance-money-coach-service.ts`, builds a minimal privacy-safe snapshot from the already-deterministic `getMonthlyReport()` (totals, top category deltas, flagged budget categories, subscriptions total, savings %-progress — all numbers/category names only; no user id, email, transaction descriptions, or notes ever leave the server) and forces a single structured tool call so the response is validated JSON, never parsed free text. **AI never calculates a total** — the system prompt states every number is ground truth the model must not recompute, and (defense in depth) the service re-derives the recommendation's `categoryId`/`categoryName`/`currentBudgetKrw` from the real budget-performance data server-side, discarding the recommendation outright if the model names a category that isn't actually flagged — only `suggestedBudgetKrw` and the rationale text are trusted from the model. The recommendation is scoped to exactly one action type (one category's budget amount) per the only worked example in AGENTS.md; the system prompt explicitly forbids suggesting a transaction delete or a savings transfer, matching "AI may NOT directly: change budget, delete transaction, create savings transfer." **No silent mutation**: Settings renders Current → Suggested → rationale with Cancel/Apply, and Apply calls the *exact same* `upsertBudget()` a human uses from the Budgets page — there is no AI-authorized write path; the model's output never touches the database directly. "Get insights" requires an explicit click (no auto-fetch on page load) since each call costs real tokens. AI Chat (a general Finance Q&A conversation, distinct from the deterministic Money Coach) was not ported — AGENTS.md's example is specifically the Money Coach's summarize-and-recommend shape, and an open-ended chat surface raises the same arithmetic/privacy questions without a worked example to follow. |
| PWA / offline | Money Flow's `app/manifest.ts` + `public/sw.js` | `app/manifest.ts`, `public/sw.js`, `app/icons/icon-{192,512}/route.tsx`, `components/pwa-register.tsx` | Installable app shell + read-only recent cached data | MATCH | DONE | **App-wide, not Finance-owned** — per AGENTS.md's explicit "Do not make Finance a separate PWA," this phase lives at the repo root: `app/manifest.ts` names the product "Luyra" with shortcuts to Today/Tasks/Finance/Habits (not Finance-only shortcuts, unlike Money Flow's Add-Transaction/Savings/Analytics set), and `public/sw.js` precaches the app shell across every top-level domain route, not just `/finance`. Assessment: Money Flow's PWA pattern (network-first-with-cache-fallback for API GETs, stale-while-revalidate for pages, **never intercepting a non-GET request**) is still the right shape — ported near-verbatim — but re-scoped from a Finance-only shell to the whole app, exactly as instructed. **No offline writes**: the service worker's fetch handler returns early for any non-GET request, so a mutation while offline just fails with an ordinary network error — there is no sync queue and this phase doesn't add one, per "Do not allow risky offline financial writes without a proper sync queue." Icons are generated at request time via `next/og`'s `ImageResponse` (Satori, bundled with Next.js) rather than needing a design asset or an image-processing binary — a placeholder "L" wordmark, easy to swap for real branding later. **A real, unrelated bug found and fixed along the way**: `proxy.ts` (Next.js 16 renamed `middleware.ts` → `proxy.ts` — exactly the kind of convention drift `AGENTS.md`'s top-of-repo warning calls out) excluded `favicon.ico` from its auth-redirect matcher but not the new manifest/icon/service-worker paths, so browsers fetching them got redirected to `/login` instead of the actual asset — verified by curling `/manifest.webmanifest` before and after the fix. The matcher now also excludes `manifest.webmanifest`, `icons`, and `sw.js`; `/today` and other real protected routes were re-verified to still redirect correctly. |

## Dashboard component decisions

| Money Flow component | Classification | Native Finance decision |
|----------------------|----------------|-------------------------|
| `SummaryCards` | Presentational with parent-provided totals | Adapt the hierarchy; replace Framer Motion and Money Flow formatting with Luyra primitives. |
| `DailyBudgetPill` | Presentational; parent owns deterministic math | Adapt the presentation and calculate the values in the Luyra Finance service. |
| `BudgetAlerts` | Presentational with navigation/dismiss state | Adapt from native `BudgetPerformance`; use `/finance/budgets`. |
| `BudgetReviewPrompt` | Presentational with local dismissal logic | Adapt as a native monthly-review entry point; no Money Flow storage key. |
| `CategoriesPanel` | Presentational but Recharts-dependent | Adapt the category hierarchy; use a lightweight native visualization for Overview. |
| `AnalyticsTabs` | Presentational interaction plus lazy chart loading | Do not copy the dashboard tab-within-tab structure into Finance Overview. |
| `TrendsPanel` | Presentational and Recharts-dependent | Adapt the daily trend with native aggregate data; avoid a dependency until richer Analytics needs it. |
| `RecentActivity` | Presentational with Money Flow transaction shape | Adapt through native camel-case Finance DTOs and native route links. |
| `Intelligence` | Deterministic calculations plus unsupported quick-add templates | Defer forecast copy and quick add until the native response/Transactions work supports them. |
| `MoneyCoach` | AI endpoints, persisted insight actions, and direct Supabase budget writes | Do not port. |

## Current-session boundary

Prior sessions implemented `/finance` Overview through `/finance/settings`
(see their rows above) — the full core UI-parity tier is `MATCH`. Phase 8
(Recurring transactions) was explicitly skipped by request; Phases 9
(Transaction templates), 10 (Multi-currency), 11 (Reports), 14 (Export), 12
(Telegram — account linking + manual report delivery, `PARTIAL`), 13
(Email reports — manual delivery, `PARTIAL`, see its row above), 15 (AI
Foundation + Money Coach), and 16 (PWA/offline — app-wide, see its row
above) are all done, most out of mission order at explicit request.
Recurring transactions remains `DEFERRED` by request, not by default
ordering — it's the only phase left from the full 1–16 list. Phase 15 was
done before Phase 16 (though after Reports), consistent with AGENTS.md's own
instruction to do AI "LAST among core Finance functionality" — every prior
phase's deterministic data (Reports' `getMonthlyReport()` especially) fed
directly into the Money Coach's snapshot rather than needing to be rebuilt.

A concurrent session was found actively reworking `/finance` Overview
(new `SavingsRateHealth`/`BudgetHealth` types, a rewritten
`finance-overview.tsx`, and new `tinted`/`surfaceTone` variants on
`FinanceMetricCard`) while this session's Telegram and AI work were in
progress. Left entirely untouched and unreverted — neither was needed for
either phase, and `tsc`/`lint`/`build` all passed cleanly against that
in-progress state throughout, so the efforts never collided.

A concurrent session was found actively reworking `/finance` Overview
(new `SavingsRateHealth`/`BudgetHealth` types, a rewritten
`finance-overview.tsx`, and new `tinted`/`surfaceTone` variants on
`FinanceMetricCard`) while this session's Telegram work was in progress.
Left entirely untouched and unreverted — those files weren't needed for
Telegram, and `tsc`/`lint`/`build` all passed cleanly against that
in-progress state both before and after this session's changes, so the two
efforts didn't collide.

Merchant alias grouping (`finance_transaction_description_aliases`) was not
touched — detection still groups by exact normalized description, per the
existing "no fuzzy grouping without a native alias domain" decision.

Only two sessions have added a third-party dependency, both because there
was no way around it: `@anthropic-ai/sdk` (AI Money Coach) and `resend`
(Email reports). Analytics did not need Recharts despite the pre-approval
(see the Analytics trends row), and Email did not need
`@react-email/components` — a hand-rolled escaped-HTML template, the same
approach already used for the Telegram message, covered it. AI Money
Coach's "Apply recommendation" mutation path, Money Flow's own review
workflow, Savings auto-plan/reminder automation, Money Flow authentication,
browser Supabase CRUD, category-type classification (`CategoryTypesSheet`),
and merchant alias grouping (`MerchantNamesSheet`) remain intentionally
excluded.

## Incidental fixes made while working in shared files

- `db/migrations/007_finance_savings_contributions_cascade.sql` — a savings
  goal with any contribution history could not be deleted (FK violation, no
  `ON DELETE CASCADE`). Not yet applied to the database; see `db/README.md`.
- `tsconfig.json` now excludes `build/` — a separate git worktree
  (`build/goal-task-worktree`, branch `feat/hengo-goal-task-integration`) was
  nested inside this repo and, because nothing excluded it, `tsc --noEmit`
  from the repo root was type-checking its files too. Its `@/*` imports
  resolved against *this* tree's `types/finance.ts` and `lib/*` instead of
  its own, producing errors that had nothing to do with either branch's real
  code. This looks like another in-progress session's worktree — it was left
  untouched, only excluded from this repo's own type-check.
